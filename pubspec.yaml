name: thinkhub_client
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.13.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_localized_locales: ^2.0.1
  intl: ^0.17.0
  crdt:
    path: packages/crdt
  slate:
    path: packages/slate
  protocol:
    path: packages/protocol
  common:
    path: packages/common
  cupertino_icons: ^1.0.4
  file_picker: ^4.2.2+1
  flutter_keyboard_visibility: ^5.0.3
  adaptive_breakpoints: ^0.1.0
  animations: ^2.0.1
  web_socket_channel: ^2.1.0
  device_info_plus: ^3.2.1
  dual_screen: ^1.0.3

  image_picker: ^0.8.2
  tuple: ^2.0.0
  dartx: ^1.0.0
  quiver: ^3.0.1+1
  fixnum: ^1.0.0
  uuid: ^3.0.4
  flutter_colorpicker: ^1.0.3
  filesystem_picker: ^2.0.1
  path: ^1.8.0

  logger: ^1.1.0
  flutter_webrtc: ^0.8.9
  xml2json: ^5.3.1
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  stash_hive: ^4.0.1
  json_annotation: ^4.3.0
  json_serializable: ^6.0.1

  # 错误处理的函数式编程
  dartz: ^0.10.0
  # a package containing annotations for freezed.
  freezed_annotation: ^1.0.0
  # 服务定位器
  get_it: ^7.2.0
  equatable: ^2.0.3
  flutter_riverpod: ^1.0.0
  provider: ^6.0.3

  flutter_svg: ^1.0.3
  syncfusion_flutter_pdfviewer: ^19.3.44-beta

  flex_color_scheme: ^5.0.1
  flex_color_picker: ^2.5.0

  flutter_staggered_grid_view: ^0.6.1

  google_fonts: ^2.1.0
  material_design_icons_flutter: ^5.0.6295
  path_provider: ^2.0.7
  shared_preferences: ^2.0.9
  string_scanner: ^1.1.0

  url_launcher: ^6.0.13

  ktx: ^1.1.6

  flutter_libphonenumber: ^1.2.1

  flutter_sound: ^9.1.2
  keframe: ^3.0.0

dev_dependencies:
  # the link to run code-generators
  build_runner: ^2.1.5 # flutter pub run build_runner build --delete-conflicting-outputs
  # code generator
  freezed: ^1.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^0.2.0
  flutter_lints: ^1.0.4
  dart_code_metrics: ^4.7.0 # Check unused files flutter pub run dart_code_metrics:metrics check-unused-files lib

  # pub run dependency_validator
  dependency_validator: ^3.1.0
  hive_generator: ^1.1.1
  macos_ui: ^0.10.0+1

flutter:
  generate: true # disable for build_runner
  uses-material-design: true
  assets:
    - assets/images/
    - assets/pdf/
